#include <unistd.h>
#include <fcntl.h>
int main() {
	int tmp;
	tmp = dup(1);
	int fd = open("file", O_TRUNC | O_CREAT | O_RDWR);
	dup2(fd, 1);
	write(1, "HELLO", 5);
	dup2(tmp, 1);
	write(1, "HI", 2);

}